services:
  ui:
    build:
      context: .
      dockerfile_inline: |
        FROM node:22-alpine
        WORKDIR /app
        COPY . .
        ENV LOG_DIR=/data/logs PORT=80
        CMD ["node", "server.js"]
    ports:
      - "8083:80"
    volumes:
      - ..:/data:ro
